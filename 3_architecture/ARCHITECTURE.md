# 🏗️ Архитектура Email Outreach для 1200 адресов

**Polza Outreach Engine - Техническое решение**

---

## 📋 Требования

- **1200 email-адресов** для массовых рассылок
- **Несколько клиентов** и направлений
- **Минимальная стоимость** инфраструктуры
- **Высокая отказоустойчивость**
- **Мониторинг и аналитика**

---

## 🎯 Предлагаемая архитектура

### Общая схема

┌─────────────────┐
│  Control Panel  │ ← Управление через веб-интерфейс
│   (FastAPI)     │
└────────┬────────┘
│
v
┌─────────────────────────────────────────────┐
│          PostgreSQL Database                │
│  ┌─────────────┬──────────────┬──────────┐ │
│  │  Campaigns  │  Email Pool  │  Stats   │ │
│  └─────────────┴──────────────┴──────────┘ │
└────────┬────────────────────────────────────┘
│
v
┌─────────────────────────────────────────────┐
│          Redis Queue (Celery)               │
│     Очередь задач + Rate Limiting           │
└────────┬────────────────────────────────────┘
│
v
┌─────────────────────────────────────────────┐
│         Worker Pool (3-5 воркеров)          │
│  ┌──────────┬──────────┬──────────┐         │
│  │ Worker 1 │ Worker 2 │ Worker 3 │         │
│  └──────────┴──────────┴──────────┘         │
└────────┬────────────────────────────────────┘
│
v
┌─────────────────────────────────────────────┐
│    Email Sending Layer (Rotation)           │
│  ┌───────────┬───────────┬────────────┐     │
│  │ Resend.com│SendGrid.ru│ SMTP Pool  │     │
│  │  (400)    │  (400)    │   (400)    │     │
│  └───────────┴───────────┴────────────┘     │
└────────┬────────────────────────────────────┘
│
v
┌─────────────────────────────────────────────┐
│   Monitoring & Analytics                    │
│  ┌──────────┬──────────┬──────────┐         │
│  │ Grafana  │ Sentry   │ Logs     │         │
│  └──────────┴──────────┴──────────┘         │
└─────────────────────────────────────────────┘

---

## 🔧 Технический стек

### Backend
- **Python 3.11+** с FastAPI
- **Celery** для асинхронных задач
- **PostgreSQL** для хранения данных
- **Redis** для очередей и кэширования

### Email Sending
- **Resend.com** - основной провайдер (400 адресов)
- **SendGrid** - резервный провайдер (400 адресов)
- **Собственный SMTP-пул** - 5-10 доменов на VPS (400 адресов)

### Infrastructure
- **Hetzner Cloud** или **DigitalOcean** - VPS
- **Cloudflare** - DNS + защита от DDoS
- **Uptime Robot** - мониторинг доступности

---

## 📊 Распределение нагрузки

### Разделение 1200 адресов:
```python
SENDING_STRATEGY = {
    'resend': {
        'limit': 400,
        'daily_limit': 10000,
        'use_for': ['high_priority', 'cold_outreach']
    },
    'sendgrid': {
        'limit': 400,
        'daily_limit': 100,
        'use_for': ['newsletters', 'follow_ups']
    },
    'smtp_pool': {
        'limit': 400,
        'daily_limit': 5000,
        'use_for': ['bulk', 'testing']
    }
}
```

### Ротация доменов:

- **10 доменов** для собственного SMTP-пула
- По **40 адресов** на домен
- **Warmup period**: 2 недели на каждый новый домен
- **Sending limits**: max 50 писем/день с каждого адреса

---

## ⚡ Отказоустойчивость

### 1. Failover Strategy
```python
def send_email(recipient, content):
    providers = ['resend', 'sendgrid', 'smtp_pool']
    
    for provider in providers:
        try:
            result = send_via_provider(provider, recipient, content)
            if result.success:
                return result
        except Exception as e:
            log_error(provider, e)
            continue
    
    # В очередь на retry
    queue_for_retry(recipient, content, delay=3600)
```

### 2. Health Checks
```python
# Каждые 5 минут проверяем провайдеры
HEALTH_CHECKS = {
    'resend': check_resend_api(),
    'sendgrid': check_sendgrid_api(),
    'smtp_pool': check_smtp_servers()
}

# Автоматическое переключение трафика
if not HEALTH_CHECKS['resend']:
    redistribute_load('resend', to=['sendgrid', 'smtp_pool'])
```

---

## 📈 Мониторинг

### Ключевые метрики:

- **delivery_rate** - Процент доставленных писем
- **open_rate** - Процент открытий
- **bounce_rate** - Процент отскоков
- **spam_rate** - Попадание в спам
- **queue_size** - Размер очереди
- **worker_health** - Статус воркеров

### Dashboards (Grafana):

1. **Real-time dashboard**: текущая загрузка, очереди, ошибки
2. **Campaign dashboard**: статистика по кампаниям
3. **Provider dashboard**: статистика по провайдерам

---

## 💰 Оценка стоимости (месяц)

### Email Providers:

| Провайдер | Тариф | Адресов | Цена/мес |
|-----------|-------|---------|----------|
| Resend.com | Pro | 400 | $20 |
| SendGrid | Free → Essentials | 400 | $0 → $20 |
| SMTP Pool (VPS) | 10 доменов | 400 | $40 |
| **Итого провайдеры** | | **1200** | **$60-80** |

### Infrastructure:

| Сервис | Конфигурация | Цена/мес |
|--------|--------------|----------|
| VPS (Hetzner) | 4 vCPU, 8GB RAM | $8 |
| PostgreSQL | На VPS | $0 |
| Redis | На VPS | $0 |
| Cloudflare | DNS + защита | $0 |
| S3 Storage | 50GB бэкапы | $1 |
| **Итого** | | **$9** |

### **ОБЩАЯ СТОИМОСТЬ: $69-89/месяц**

---

## 🚨 Риски и их решения

### Риск 1: Попадание в спам-листы

**Решение:**
- SPF, DKIM, DMARC настройки для всех доменов
- Постепенный прогрев доменов (warmup)
- Мониторинг репутации через MXToolbox
- Double opt-in для всех подписчиков

### Риск 2: Блокировка провайдера

**Решение:**
- 3 независимых провайдера
- Автоматический failover
- Регулярные health checks

### Риск 3: Превышение лимитов

**Решение:**
- Rate limiting в Redis
- Динамическая очередь с приоритетами
- Мониторинг квот провайдеров

### Риск 4: Потеря данных

**Решение:**
- Ежедневные бэкапы БД
- Point-in-time recovery
- Репликация данных

---

## 🔄 Workflow отправки
```python
# 1. Создание кампании
campaign = create_campaign(
    name="Cold Outreach Q1",
    recipients=1200
)

# 2. Валидация адресов
valid_emails = validate_email_batch(campaign.recipients)

# 3. Распределение по провайдерам
distribute_emails(
    resend=380,
    sendgrid=380,
    smtp_pool=380
)

# 4. Добавление в очередь
for email in valid_emails:
    queue.add_task(send_email, email=email)

# 5. Tracking
track_events(['sent', 'delivered', 'opened', 'clicked'])
```

---

## 🎯 Масштабирование

При росте до **5000+ адресов**:

1. **Horizontal scaling**: добавить 2-3 VPS ноды
2. **Managed services**: PostgreSQL → Managed Database
3. **Email провайдеры**: Amazon SES, Postmark, Mailgun

**Стоимость при 5000 адресов: ~$200-300/мес**

---

## 📚 Итоги

✅ Архитектура поддерживает 1200 адресов  
✅ Стоимость: $69-89/месяц  
✅ Высокая отказоустойчивость  
✅ Масштабируемость до 5000+ адресов  
✅ Полный мониторинг и аналитика  

---

**Автор:** Анастасия Станиславовна  
**Дата:** 07.02.2026

